name: Build Matrix Reusable

on:
  workflow_call:
    inputs:
      checkout_ref:
        description: Optional git ref to checkout
        required: false
        default: ''
        type: string
      build_type:
        description: CMake build type for Unix builds
        required: false
        default: Debug
        type: string
      windows_preset:
        description: CMake preset used for Windows configure/build
        required: false
        default: windows-msvc-ninja-debug
        type: string
      windows_artifact_dir:
        description: Build directory that contains Windows output artifacts
        required: false
        default: build/windows-msvc-ninja-debug
        type: string
      run_tests:
        description: Run ctest for matrix entries that do not opt out
        required: false
        default: true
        type: boolean
      artifact_prefix:
        description: Optional artifact name prefix
        required: false
        default: ''
        type: string
      cache_key_prefix:
        description: Optional cache key prefix
        required: false
        default: ''
        type: string
      package_with_cpack:
        description: Build platform packages with CPack
        required: false
        default: false
        type: boolean
      sign_windows_artifacts:
        description: Sign Windows binaries and installer with Azure Artifact Signing when package_with_cpack is enabled
        required: false
        default: false
        type: boolean
      notarize_macos:
        description: Notarize macOS package when package_with_cpack is enabled and credentials are available
        required: false
        default: false
        type: boolean
    secrets:
      MACOS_NOTARIZATION_USERNAME:
        required: false
      MACOS_NOTARIZATION_TEAM_ID:
        required: false
      MACOS_NOTARIZATION_PASSWORD:
        required: false
      AZURE_TENANT_ID:
        required: false
      AZURE_CLIENT_ID:
        required: false
      AZURE_CLIENT_SECRET:
        required: false
      AZURE_ENDPOINT:
        required: false
      AZURE_TRUSTED_SIGNING_ACCOUNT_NAME:
        required: false
      AZURE_CERTIFICATE_PROFILE_NAME:
        required: false

permissions:
  contents: read

jobs:
  build:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: linux-x86_64
            os: ubuntu-latest
            platform: linux
            arch: x86_64
            skip_tests: false
          - name: linux-i686
            os: ubuntu-latest
            platform: linux
            arch: i686
            skip_tests: true
          - name: linux-armv7l
            os: ubuntu-latest
            platform: linux
            arch: armv7l
            skip_tests: true
          - name: linux-aarch64
            os: ubuntu-latest
            platform: linux
            arch: aarch64
            skip_tests: true
          - name: windows-x64
            os: windows-latest
            platform: windows
            arch: x64
            msvc_arch: x64
            skip_tests: false
          - name: windows-x86
            os: windows-latest
            platform: windows
            arch: x86
            msvc_arch: x86
            skip_tests: true
          - name: windows-arm64ec
            os: windows-latest
            platform: windows
            arch: arm64ec
            msvc_arch: amd64_arm64
            skip_tests: true
          - name: macos-x86_64
            os: macos-latest
            platform: macos
            arch: x86_64
            skip_tests: true
          - name: macos-arm64
            os: macos-latest
            platform: macos
            arch: arm64
            skip_tests: false
    env:
      ARCH: ${{ matrix.arch }}
      MACOS_NOTARIZATION_USERNAME: ${{ secrets.MACOS_NOTARIZATION_USERNAME }}
      MACOS_NOTARIZATION_TEAM_ID: ${{ secrets.MACOS_NOTARIZATION_TEAM_ID }}
      MACOS_NOTARIZATION_PASSWORD: ${{ secrets.MACOS_NOTARIZATION_PASSWORD }}
      AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}

    steps:
      - name: Checkout (default ref)
        if: inputs.checkout_ref == ''
        uses: actions/checkout@v4

      - name: Checkout (requested ref)
        if: inputs.checkout_ref != ''
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.checkout_ref }}

      - name: Setup Homebrew Unix
        if: runner.os != 'Windows'
        uses: Homebrew/actions/setup-homebrew@main

      - name: Install Unix Homebrew packages
        if: runner.os != 'Windows'
        shell: bash
        run: |
          set -euo pipefail

          brew update
          brew install cmake
          echo "$(brew --prefix cmake)/bin" >> "$GITHUB_PATH"
          cmake --version

      - name: Install CMake and NSIS Windows
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'

          winget install --id Kitware.CMake --exact --source winget --accept-package-agreements --accept-source-agreements --silent --disable-interactivity --force

          $cmakePaths = @(
            'C:\Program Files\CMake\bin'
          )

          foreach ($pathEntry in $cmakePaths) {
            $pathEntry | Out-File -FilePath $env:GITHUB_PATH -Append -Encoding utf8
            if (-not (($env:Path -split ';') -contains $pathEntry)) {
              $env:Path = "$pathEntry;$env:Path"
            }
          }

          if ('${{ inputs.package_with_cpack }}' -eq 'true') {
            winget install --id NSIS.NSIS --exact --source winget --accept-package-agreements --accept-source-agreements --silent --disable-interactivity --force

            $nsisPaths = @(
              'C:\Program Files\NSIS\Bin',
              'C:\Program Files (x86)\NSIS\Bin',
              "$env:LOCALAPPDATA\Programs\NSIS\Bin"
            )

            foreach ($pathEntry in $nsisPaths) {
              $pathEntry | Out-File -FilePath $env:GITHUB_PATH -Append -Encoding utf8
              if (-not (($env:Path -split ';') -contains $pathEntry)) {
                $env:Path = "$pathEntry;$env:Path"
              }
            }
          }

          cmake --version

          if ('${{ inputs.package_with_cpack }}' -eq 'true') {
            makensis /VERSION
          }

      - name: Cache CMake deps
        uses: actions/cache@v4
        with:
          path: |
            build/_deps
          key: >-
            ${{ inputs.cache_key_prefix }}${{ runner.os }}-${{ matrix.arch }}-${{ hashFiles('CMakeLists.txt', 'cmake/**/*.cmake') }}

      - name: Setup Linux cross dependencies
        if: matrix.platform == 'linux'
        shell: bash
        run: |
          set -euo pipefail
          configure_apt_sources() {
            local target_arch="$1"
            . /etc/os-release

            if [ -f /etc/apt/sources.list.d/ubuntu.sources ]; then
              # Keep native packages on archive.ubuntu.com and use ports only for ARM targets.
              # Avoid security/backports suites for cross-arch parity on ubuntu-latest.
              printf '%s\n' \
                'Types: deb' \
                'Architectures: amd64 i386' \
                'URIs: http://archive.ubuntu.com/ubuntu/' \
                "Suites: ${VERSION_CODENAME} ${VERSION_CODENAME}-updates" \
                'Components: main universe restricted multiverse' \
                'Signed-By: /usr/share/keyrings/ubuntu-archive-keyring.gpg' \
                | sudo tee /etc/apt/sources.list.d/ubuntu.sources > /dev/null

              sudo rm -f /etc/apt/sources.list.d/ubuntu-ports.sources
              if [[ "$target_arch" =~ ^(arm64|armhf)$ ]]; then
                printf '%s\n' \
                  'Types: deb' \
                  "Architectures: ${target_arch}" \
                  'URIs: http://ports.ubuntu.com/ubuntu-ports/' \
                  "Suites: ${VERSION_CODENAME} ${VERSION_CODENAME}-updates" \
                  'Components: main universe restricted multiverse' \
                  'Signed-By: /usr/share/keyrings/ubuntu-archive-keyring.gpg' \
                  | sudo tee /etc/apt/sources.list.d/ubuntu-ports.sources > /dev/null
              fi
            elif [ -f /etc/apt/sources.list ]; then
              printf '%s\n' \
                "deb [arch=amd64,i386] http://archive.ubuntu.com/ubuntu/ ${VERSION_CODENAME} main universe restricted multiverse" \
                "deb [arch=amd64,i386] http://archive.ubuntu.com/ubuntu/ ${VERSION_CODENAME}-updates main universe restricted multiverse" \
                | sudo tee /etc/apt/sources.list > /dev/null

              sudo rm -f /etc/apt/sources.list.d/ports.list
              if [[ "$target_arch" =~ ^(arm64|armhf)$ ]]; then
                printf '%s\n' \
                  "deb [arch=${target_arch}] http://ports.ubuntu.com/ubuntu-ports/ ${VERSION_CODENAME} main universe restricted multiverse" \
                  "deb [arch=${target_arch}] http://ports.ubuntu.com/ubuntu-ports/ ${VERSION_CODENAME}-updates main universe restricted multiverse" \
                  | sudo tee /etc/apt/sources.list.d/ports.list > /dev/null
              fi
            fi
          }

          install_deps() {
            local target_arch="$1"; shift
            local native=("$@" php-cli qemu-user-binfmt)
            local target=(
              libboost-dev libcurl4-openssl-dev libsqlite3-dev libssl-dev
              libxml2-dev zlib1g-dev
            )

            sudo dpkg --add-architecture "$target_arch"
            configure_apt_sources "$target_arch"
            sudo rm -rf /var/lib/apt/lists/*
            sudo apt-get update -qq
            sudo apt-get install -y -qq aptitude > /dev/null
            sudo aptitude install -yR "${native[@]}" "${target[@]/%/:$target_arch}" > /dev/null
            sudo aptitude install -yR "${native[@]}" > /dev/null
          }

          case "$ARCH" in
            x86_64)
              install_deps amd64
              ;;
            i686)
              install_deps i386 g++-multilib
              {
                echo "TOOLCHAIN=${GITHUB_WORKSPACE}/cmake/linux-cross.cmake"
                echo "TOOLCHAIN_PREFIX=i386-linux-gnu"
              } >> "$GITHUB_ENV"
              ;;
            armv7l)
              install_deps armhf g++-arm-linux-gnueabihf
              {
                echo "TOOLCHAIN=${GITHUB_WORKSPACE}/cmake/linux-cross.cmake"
                echo "TOOLCHAIN_PREFIX=arm-linux-gnueabihf"
              } >> "$GITHUB_ENV"
              ;;
            aarch64)
              install_deps arm64 g++-aarch64-linux-gnu
              {
                echo "TOOLCHAIN=${GITHUB_WORKSPACE}/cmake/linux-cross.cmake"
                echo "TOOLCHAIN_PREFIX=aarch64-linux-gnu"
              } >> "$GITHUB_ENV"
              ;;
            *)
              echo "Unsupported ARCH: $ARCH" >&2
              exit 1
              ;;
          esac

      - name: Setup macOS build environment
        if: matrix.platform == 'macos'
        shell: bash
        run: |
          set -euo pipefail

          echo "DEPLOY_TARGET=11.0" >> "$GITHUB_ENV"

          if [ -d /Library/Frameworks/Mono.framework ]; then
            sudo rm -rf /Library/Frameworks/Mono.framework
          fi

      - name: Setup MSVC developer command prompt
        if: matrix.platform == 'windows'
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: ${{ matrix.msvc_arch }}

      - name: Setup ARM64EC flags
        if: matrix.platform == 'windows' && matrix.arch == 'arm64ec'
        shell: pwsh
        run: |
          'CFLAGS=-arm64EC' | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
          'CXXFLAGS=-arm64EC' | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8

      - name: Configure Unix
        if: runner.os != 'Windows'
        shell: bash
        run: |
          set -euo pipefail
          cmake_args=(
            -B build
            -DCMAKE_BUILD_TYPE=${{ inputs.build_type }}
            -DCMAKE_OSX_ARCHITECTURES="$ARCH"
          )

          if [ -n "${DEPLOY_TARGET:-}" ]; then
            cmake_args+=("-DCMAKE_OSX_DEPLOYMENT_TARGET=${DEPLOY_TARGET}")
          fi

          if [ -n "${TOOLCHAIN:-}" ]; then
            cmake_args+=("-DCMAKE_TOOLCHAIN_FILE=${TOOLCHAIN}")
          fi

          cmake "${cmake_args[@]}"

      - name: Configure Windows
        if: runner.os == 'Windows'
        shell: pwsh
        run: cmake --preset ${{ inputs.windows_preset }}

      - name: Build Unix
        if: runner.os != 'Windows'
        shell: bash
        run: cmake --build build

      - name: Setup macOS notarytool keychain profile
        if: matrix.platform == 'macos' && inputs.package_with_cpack && inputs.notarize_macos && env.MACOS_NOTARIZATION_USERNAME != ''
        shell: bash
        run: |
          set -euo pipefail
          xcrun notarytool store-credentials "notarytool-profile" \
            --apple-id "$MACOS_NOTARIZATION_USERNAME" \
            --team-id "$MACOS_NOTARIZATION_TEAM_ID" \
            --password "$MACOS_NOTARIZATION_PASSWORD"

      - name: Configure macOS notarization profile
        if: matrix.platform == 'macos' && inputs.package_with_cpack && inputs.notarize_macos && env.MACOS_NOTARIZATION_USERNAME != ''
        shell: bash
        run: |
          set -euo pipefail
          cmake -B build -DNOTARIZE_KEYCHAIN_PROFILE=notarytool-profile

      - name: Build Unix package
        if: runner.os != 'Windows' && inputs.package_with_cpack
        shell: bash
        run: cpack --config build/CPackConfig.cmake

      - name: Notarize macOS package
        if: matrix.platform == 'macos' && inputs.package_with_cpack && inputs.notarize_macos && env.MACOS_NOTARIZATION_USERNAME != ''
        shell: bash
        run: cmake --build build --target notarize

      - name: macOS notarization skipped notice
        if: matrix.platform == 'macos' && inputs.package_with_cpack && inputs.notarize_macos && env.MACOS_NOTARIZATION_USERNAME == ''
        shell: bash
        run: |
          echo "Notarization requested, but MACOS_NOTARIZATION_USERNAME is empty. Package was built without notarization."

      - name: Build Windows
        if: runner.os == 'Windows'
        shell: pwsh
        run: cmake --build --preset ${{ inputs.windows_preset }}

      - name: Sign Windows binaries
        if: matrix.platform == 'windows' && inputs.package_with_cpack && inputs.sign_windows_artifacts && env.AZURE_TENANT_ID != ''
        uses: azure/artifact-signing-action@v1
        with:
          azure-tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          azure-client-id: ${{ secrets.AZURE_CLIENT_ID }}
          azure-client-secret: ${{ secrets.AZURE_CLIENT_SECRET }}
          endpoint: ${{ secrets.AZURE_ENDPOINT }}
          trusted-signing-account-name: ${{ secrets.AZURE_TRUSTED_SIGNING_ACCOUNT_NAME }}
          certificate-profile-name: ${{ secrets.AZURE_CERTIFICATE_PROFILE_NAME }}
          files-folder: ${{ github.workspace }}\${{ inputs.windows_artifact_dir }}
          files-folder-filter: dll,exe
          files-folder-recurse: false
          file-digest: SHA256
          timestamp-rfc3161: http://timestamp.acs.microsoft.com
          timestamp-digest: SHA256

      - name: Build Windows package
        if: runner.os == 'Windows' && inputs.package_with_cpack
        shell: pwsh
        run: cpack --config ${{ inputs.windows_artifact_dir }}/CPackConfig.cmake

      - name: Sign Windows installer
        if: matrix.platform == 'windows' && inputs.package_with_cpack && inputs.sign_windows_artifacts && env.AZURE_TENANT_ID != ''
        uses: azure/artifact-signing-action@v1
        with:
          azure-tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          azure-client-id: ${{ secrets.AZURE_CLIENT_ID }}
          azure-client-secret: ${{ secrets.AZURE_CLIENT_SECRET }}
          endpoint: ${{ secrets.AZURE_ENDPOINT }}
          trusted-signing-account-name: ${{ secrets.AZURE_TRUSTED_SIGNING_ACCOUNT_NAME }}
          certificate-profile-name: ${{ secrets.AZURE_CERTIFICATE_PROFILE_NAME }}
          files-folder: ${{ github.workspace }}\${{ inputs.windows_artifact_dir }}\packages
          files-folder-filter: exe
          files-folder-recurse: false
          file-digest: SHA256
          timestamp-rfc3161: http://timestamp.acs.microsoft.com
          timestamp-digest: SHA256

      - name: Test Unix
        if: runner.os != 'Windows' && inputs.run_tests && !matrix.skip_tests
        shell: bash
        run: ctest --test-dir build --output-on-failure

      - name: Test Windows
        if: runner.os == 'Windows' && inputs.run_tests && !matrix.skip_tests
        shell: pwsh
        run: ctest --test-dir ${{ inputs.windows_artifact_dir }} --output-on-failure

      - name: Upload Linux artifacts
        if: always() && matrix.platform == 'linux'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.artifact_prefix != '' && format('{0}-{1}', inputs.artifact_prefix, matrix.name) || matrix.name }}
          path: |
            build/*.so
            build/packages/*.deb
          if-no-files-found: warn

      - name: Upload macOS artifacts
        if: always() && matrix.platform == 'macos'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.artifact_prefix != '' && format('{0}-{1}', inputs.artifact_prefix, matrix.name) || matrix.name }}
          path: |
            build/*.dylib
            build/packages/*.pkg
          if-no-files-found: warn

      - name: Upload Windows artifacts
        if: always() && matrix.platform == 'windows'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.artifact_prefix != '' && format('{0}-{1}', inputs.artifact_prefix, matrix.name) || matrix.name }}
          path: |
            ${{ inputs.windows_artifact_dir }}/*.dll
            ${{ inputs.windows_artifact_dir }}/*.pdb
            ${{ inputs.windows_artifact_dir }}/packages/*.exe
          if-no-files-found: warn
